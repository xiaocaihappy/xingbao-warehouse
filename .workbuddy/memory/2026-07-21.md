# 2026-07-21 工作日志

## 修复：Logo 显示异常 + 人员默认项

### 1. Logo 恢复完整显示
- **根因**：之前 Python 处理时将 RGB<30 的像素全设为透明，把"星堡积木"黑色字体也透明掉了，logo 只剩蓝圈
- **修复**：从 `E:\Desktop\XBlogo.png` 恢复原始文件，只透明化白色背景（RGB>=240），保留黑色字体（8328个黑色像素完整保留）
- 同步覆盖到 `app/XBlogo.png` 和 `app/public/XBlogo.png`

### 2. 人员管理"默认"项解除保护
- **根因**：`is_default=true` 的人员被锁定不可删除，导致默认人员永远无法移除
- **修复**：
  - `Storage.jsx`：移除 `is_default` 检查，所有人员均可删除（保留 confirm 确认弹窗）
  - `supabase.js`：`deleteStaffMember` 移除 `isDefault` 参数，直接执行删除
  - 人员列表 UI 移除"默认"标签，统一显示删除按钮

### 本地构建
- `app/release-build/xingbao-warehouse-Setup-1.1.20.exe`（104.9 MB）
