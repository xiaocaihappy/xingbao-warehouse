# 2026-07-30 工作记录

## 新增功能 (v1.1.26, 已打包+提交 3f80cf6)
四个需求全部实现，未改动任何已有 class/布局（仅新增 class）：
1. **存储系统切页输入保留**：`Storage.jsx` 用模块级 `storageFormDraft` + `formRef` + `justSavedRef`。卸载时存 form，重挂载恢复；成功保存后清空草稿。
2. **图片上传优化**：选/拖图 → `compressImage`（最长边 1600px JPEG 压缩提速）→ `ImageEditor` 裁剪旋转 → 后台异步上传（`useImageUploader`）。保存时 `awaitPending()` 等待在途上传，避免存半截图。预览区支持拖拽替换。
3. **图片编辑增强**：新增 `ImageEditor.jsx`（纯 Canvas，无依赖），支持 1:1 正方形 / 自由裁切 + 旋转（±90° 与滑块）。算法：先按未旋转坐标裁剪再整体旋转，与预览一致。
4. **查询系统编辑同步**：`Query.jsx` 编辑弹窗图片区接入同一套（拖拽 + ImageEditor + 后台上传 + 保存等待在途）。

## 文件
- 新增 `app/src/components/ImageEditor.jsx`、`app/src/utils/imageUtils.js`、`app/src/utils/useImageUploader.js`
- 改动 `app/src/pages/Storage.jsx`、`app/src/pages/Query.jsx`、`app/src/index.css`、`app/package.json`

## 构建提示
- 打包命令（已验证可用）：`cd app && rm -rf dist "release-build/win-unpacked.tmp.lock" "release-build/win-unpacked" && NODE_OPTIONS="" npm run build && NODE_OPTIONS="" ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/ ./node_modules/.bin/electron-builder --win`
- 安装包：`release-build/xingbao-warehouse-Setup-1.1.26.exe`（约 105MB，已签名）。
- 发布仍卡在 GitHub 凭证（无 token/gh，MCP 仅只读），未发布。

## 查询页性能优化 (v1.1.27, 已打包+提交 5cca5ee)
用户反馈"添加数据后点开查询系统要等很久"。病根：每次打开 `fetchItems()` 都 `select('*')` 全表下载（无 limit），且列表原图 eager 加载、实时订阅全表重拉。
落地方案（A+B+C+D 全做）：
- `supabase.js`：`fetchItems` 传 `page` 时走服务端分页（`select('*',{count:'exact'}).range()`），年份用 `created_at` gte/lt；无 `page` 时保持旧全量行为（Storage 查重依赖）。新增 `fetchItemStats`（head count + 轻量 sales_channel 列做渠道分布）、`fetchFilterOptions`（去重渠道/年份，仅轻列）。
- 新增 `app/src/utils/queryCache.js`：页面缓存 + 筛选选项缓存 + `matchesFilters` 单行匹配（纯内存，不落盘）。
- `Query.jsx`：打开先用缓存秒出当前页（不转圈），后台静默刷新；搜索/渠道/年份全部走服务端；列表图 `loading=lazy` + `?width=240` 缩略图，大图弹窗用原图；实时订阅改为增量更新当前页（不再全表重拉）；导出改为点击时按需全量 `fetchItems()`。
- `Dashboard.jsx`：`loadStats` 改用 `fetchItemStats`（不再全表 select('*')）。
- 注意：导出"全部/选中"现在靠点击时全量拉取，并非缓存；"全选"仅选中当前页（行为微调，可接受）。
- 安装包：`release-build/xingbao-warehouse-Setup-1.1.27.exe`（已签名，待用户测试）。

## 工作流修正：版本号不要每次都递增
用户明确要求：**版本号只在正式发布/打包给用户当正式版时才 +1**，平时改代码、出测试包都沿用当前版本（1.1.27 作为当前测试版反复迭代）。已同步修正 MEMORY.md 的版本号惯例。

## 修复：查询编辑弹窗图片拖入/裁切/旋转失效 (commit 7847a5a)
用户反馈查询系统编辑弹窗仍无法拖入、裁切、旋转图片。修复内容：
- `Query.jsx`：把拖放接收区从仅包裹小预览图扩大到整行；预览图新增点击打开编辑器；新增"编辑图片"按钮；打开/关闭编辑弹窗时清理 `useImageUploader` 状态，避免 A 记录预览残留到 B 记录。
- `imageUtils.js`：`fileToImage` 成功后释放 objectURL（之前只失败时释放，内存泄漏）；新增 `urlToBlob` 把现有图片 URL 转为 Blob 供编辑器裁剪/旋转。
- `useImageUploader.js`：`clear()` 时释放 `localPreview` 的 objectURL。
- 版本保持 1.1.27，安装包已重新打包：`release-build/xingbao-warehouse-Setup-1.1.27.exe`。

## 修复：查询页图片放大弹窗被左侧边栏遮挡 (commit 27ed5ee)
用户反馈查询系统点击放大图片后，个别图片左侧被导航栏挡住。修复：
- `index.css`：`.image-lightbox` 从 `inset: 0`（全屏覆盖）改为 `top/right/bottom: 0` + `left: 260px`，让弹窗/遮罩只覆盖右侧内容区，避开 260px 宽的左侧导航栏。
- `.image-lightbox-content` 的 `max-width` 从 `90vw` 改为 `100%`；`.image-lightbox-img` 的 `max-width` 改为 `calc(100vw - 260px - 80px)`，确保图片按右侧可用空间重新计算尺寸。
- 版本保持 1.1.27，安装包已重新打包：`release-build/xingbao-warehouse-Setup-1.1.27.exe`。

## 去掉开屏页 + Excel 导入按模板并同步图片 (1.1.27, 已打包)
三项改动，版本号仍保持 1.1.27：
1. **去掉开屏页**：`main.js` 删除 `splashWindow` 变量、`createSplashWindow()` 函数、`ready-to-show`/开发模式的关 splash 逻辑、`THEME_FILE`/`readSavedTheme`、`theme:save` IPC；`app.whenReady` 不再先建 splash 窗口。`preload.js` 删除 `saveTheme` 暴露；`Settings.jsx` 删除 `saveTheme` 调用与"开屏页将自动跟随"文案；`package.json` build.files 移除 `splash.html`；物理删除 `app/splash.html`。
2. **Excel 导入按模板**：`Storage.jsx` `handleExcelImport` 重写——按模板表头（货架号/格子/货号/移印编号/销售列/图片/仓储人员/创建时间）动态定位列（含别名兼容），用 `sheet_to_json(header:1)` 保留行号。
3. **导入同步图片**：用 `JSZip`（已在 renderer 依赖）解压 xlsx，解析 `xl/drawings` 的图片锚点（0-based 行号）与 `xl/media` 二进制，按行号关联到数据行；内嵌图片上传到 Supabase `storage-images` 桶写 `image_url`，图片列中的 http(s) 链接也兼容。
- 关键踩坑：`exceljs` 4.4.0 读取时 `worksheet.images` 为 `undefined`、`model.media` 不带 imageId，不可靠；故改用 JSZip 解压解析（已验证可行）。
- `Storage.jsx` 新增 `import JSZip`、`supabase` 增加 `uploadImage` 引入；新增模块函数 `extractEmbeddedImages(buffer)`。
- 安装包已重新打包：`release-build/xingbao-warehouse-Setup-1.1.27.exe`。

## 修复：存储系统拖拽图片在 ImageEditor 中裂图 (commit 5ae689c)
用户反馈存储系统拖入图片后编辑器里显示裂图。根因：`ImageEditor.jsx` 调用 `fileToImage()` 取图片尺寸，而 `fileToImage` 在 `onload` 后立即 `URL.revokeObjectURL(url)`；随后 React 用 `img.src`（已失效的 blob URL）渲染 `<img>`，导致裂图。
修复：
- `ImageEditor.jsx`：组件内单独维护一个 `objectUrl` 状态，用 `URL.createObjectURL(file)` 生成并在 `useEffect` 清理时释放；`<img>` 改用该 `objectUrl` 显示，不再依赖被 `fileToImage` 过早 revoke 的 URL。
- 版本保持 1.1.27，安装包已重新打包：`release-build/xingbao-warehouse-Setup-1.1.27.exe`。
