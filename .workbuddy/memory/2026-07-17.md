# 2026-07-17 工作日志

## 用户反馈三项修复

### 1. 查询系统图片点击展开大图
- `app/src/pages/Query.jsx`：把 `expandedImage` state 从存 ID 改为存整条记录对象，新增 `<div className="image-lightbox">` 全屏预览弹窗
- `app/src/index.css`：新增 `.image-lightbox` / `.image-lightbox-img` / `.image-lightbox-close` / `.image-lightbox-info` 等样式，带缩放进入动画和关闭按钮
- 预览信息条显示 移印编号 / 货架号 / 产品货号

### 2. 查询页字段顺序（用户指定）
- 初始按 Supabase 列顺序排列，用户要求改为自定义顺序
- 最终顺序：货架号 → 格子号 → 产品货号 → 移印编号 → 图片 → 销售 → 人员 → 创建时间 → 操作
- 表头 `<th>` 和 `<td>` 同步调整

### 3. 开屏动画 logo 黑底修复
- 经 PIL 分析 `app/XBlogo.png`：原图 515x256 RGBA，但有 87323 个不透明白色像素作为背景
- 用 Python 脚本将 RGB>=245 的像素 alpha 设为 0，处理后透明像素从 42132 增至 83931
- `app/XBlogo.png` 和 `app/public/XBlogo.png` 同步处理
- 已备份为 `.bak`（后清理）

## 关键技术细节
- Supabase 列顺序通过 REST API 实际查询确认（`curl -k` 绕过本地 SSL 撤销检查问题）
- `expandedImage` 改存 item 对象而非 ID，以便预览时显示元数据
- 图片预览弹窗 z-index: 1100（高于编辑弹窗的 999），点击背景关闭，ESC 也可关闭（点击关闭按钮）

## Git 提交
- commit `0292f43`：`feat: 查询页图片点击放大、字段顺序按Supabase排列、修复开屏logo白底`
- 已推送到 `xiaocaihappy/xingbao-warehouse` main 分支
- 使用 `token.txt` 中的 GitHub PAT 通过 HTTPS 推送（SSH 主机密钥验证失败）

## 注意事项
- token.txt 不应提交到仓库（已在 .gitignore）
- 临时 .bak 文件和 Python 处理脚本已清理
- .workbuddy/memory 工作区状态文件未提交

## 发布 v1.1.18
- 自增版本号：`node scripts/inc-version.js` → 1.1.18
- commit `9bf5d1c` release: v1.1.18
- tag `v1.1.18` → 触发 GitHub Actions 自动构建发布
- 修复 `package-lock.json` 与 `package.json` 不同步导致 `npm ci` 失败的问题（重新 `npm install --package-lock-only` 后 amend commit）
- 第一次构建 .exe 上传不完整（只有 .blockmap），删除 release+tag 重新推送后成功
- Release Assets：`xingbao-warehouse-Setup-1.1.18.exe` (104.9MB) + `latest.yml` + `.blockmap`
- Release 页：https://github.com/xiaocaihappy/xingbao-warehouse/releases/tag/v1.1.18

## 存储系统页 UI 重设计（Industrial Elegance）
- `Storage.jsx` 完全重写，`app/src/index.css` 新增 567 行样式
- **设计风格**：工业暖色调（琥珀+青色点缀），类似精密工坊/仓库感
- **布局重构**：三个卡片分区（样品信息/渠道与人员/样品图片），带 Section 标题装饰线
- **字段卡片**：渐变色左边框（琥珀/青/蓝/玫瑰四色），聚焦时底部呼吸条动画
- **上传区**：支持点击选择+拖拽上传，拖入时高亮动画+光晕脉冲效果
- **图片预览**：hover 显示更换/删除操作浮层，带毛玻璃背景
- **入场动画**：staggered 交错上升动画（animation-delay 递增）
- **背景装饰**：网格纹理 + 两个浮动光晕（琥珀色右上，青色左下）
- **深色/亮色主题**：theme-light 覆盖样式已添加
- Git commit `95e7e29` feat: 优化存储系统页面UI - Industrial Elegance风格
- 已推送到 GitHub main 分支

## Bug 修复
### 1. 存储页下拉菜单定位
- `.select-field-dropdown` z-index 从 20 提升至 1000，避免被后续字段遮挡
- 原因：`.stg-card` 的 `backdrop-filter` 创建新层叠上下文，原 z-index 不足

### 2. 浅色主题查询页文字颜色
- `.cell-code` / `.cell-mono` / `.cell-time` 在 `.theme-light` 下改为 `#1a1a2e`（黑色）
- `.btn-ghost-danger` 在浅色主题下由红色改为 `#1a1a2e`

### 3. 本地打包
- 已构建 `release-build/xingbao-warehouse-Setup-1.1.20.exe`（~105 MB）
- 暂未推送到 GitHub，等待测试确认后覆盖 v1.1.20
